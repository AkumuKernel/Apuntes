// Objeto Math

//window para ver los comandos

let resultado;

resultado = Math.PI;
resultado = Math.round(4.5);
resultado = Math.ceil(2.2); //Redondea hacia arriba
resultado = Math.floor(2.9); //Redondea hacia abajo
resultado = Math.sqrt(144);
resultado = Math.abs(-155);
resultado = Math.min(11, 15, 10, 1);
resultado = Math.max(11, 15, 10, 1);
resultado = Math.random(); // 0 al 1

console.log(resultado);
